import { Card } from 'react-bootstrap'
const Footer = () => {
    return ( 
        <div className="footer">
           <Card.Footer className="text-muted">Can't have a nice footer out of the box with this stinky bootstrap</Card.Footer>
        </div>
     );
}
 
export default Footer;